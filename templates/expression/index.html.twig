{% extends 'layout.html.twig' %}

{% block title %}Expresiones{% endblock %}

{% block section %}
    <h1 style="margin-bottom: 3%;">Expresiones más buscadas</h1>

    <table id="table_id" class="display" style="width:100%">
        <thead>
            <tr>
                <th></th>
                <th>Expresión</th>
                <th>Traducción</th>
                <th>Nº búsquedas</th>
            </tr>
        </thead>
        <tbody>
            
        </tbody>
    </table>

    <script>
    $(".profile-icon").on("click", function () {
        openAside("/profile", "GET");
    })

    $(".expression-form-button").on("click", function(e) {
        e.preventDefault();
        console.log("Esta seleccionado");
        
        if ($("#auto-translate-box").is(":checked")) {
            //e.preventDefault();

            console.log("Esta seleccionado");
        }
    });

    </script>
{% endblock %}
