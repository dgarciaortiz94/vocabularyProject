{% extends 'base.html.twig' %}

{% block title %}Expression{% endblock %}

{% block body %}
    <h1>Expression</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expression.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expression.name }}</td>
            </tr>
            <tr>
                <th>Translation</th>
                <td>{{ expression.translation }}</td>
            </tr>
            <tr>
                <th>Searches</th>
                <td>{{ expression.searches }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expression_index') }}">back to list</a>

    <a href="{{ path('expression_edit', {'id': expression.id}) }}">edit</a>

    {{ include('expression/_delete_form.html.twig') }}
{% endblock %}
